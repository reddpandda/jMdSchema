/**
 * Pipeline orchestration — manifest pre-check, then per-document
 * resolve → validate, then builder.output.md generation.
 * See SCOPE.md "Builder pipeline" for the full sequence.
 *
 * <placeholder>
 */
