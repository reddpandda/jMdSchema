/**
 * Manifest/directive resolution — remark-directive AST plugin.
 * Resolves :link[X], :title[X], ::include[X] directives and
 * :hash / :commit / :built metadata tokens against the manifest.
 * Runs before the structural-validation plugin in the same pipeline.
 *
 * <placeholder>
 */
