# Migration Guides

One guide per `manifest_version` bump, for consumers who want to opt into new features. See SCOPE.md → "Manifest versioning strategy" — migration is always manual, never automatic, and no version is ever dropped, so there's no urgency to run these the moment they appear.

No migrations yet — `manifest_version` is still at its initial value (1).

<placeholder>
