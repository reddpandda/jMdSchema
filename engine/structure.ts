/**
 * Structural validation — informed by mdschema (see CREDITS.md).
 * Heading-order checking, missing-required-element detection, min/max count
 * constraints, sibling ordering, unmatched-section handling via the
 * allow-additional flag, native heading-hygiene checks.
 *
 * <placeholder>
 */
