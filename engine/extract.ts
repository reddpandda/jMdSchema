/**
 * Typed field/frontmatter extraction — adapted from a narrow, rewritten
 * slice of mdshape's primitives and combinators (see CREDITS.md).
 * Also reused to validate the manifest's own schema.
 *
 * <placeholder>
 */
